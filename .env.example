# Supabase config (Phase 2 cloud accounts).
# Copy to `.env` for local dev. These are PUBLIC values, safe to expose in the
# client bundle — Row-Level Security protects the data, not the key.
# Get them from Supabase: Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
